struct Vector3 {
    x f32,
    y f32,
    z f32,
}

fun print_vec(v: Vector3) >> void {
    @printf("%f %f %f\n", v.x, v.y, v.z);
}

fun main() >> i32 {
    print_vec(Vector3{x = 1.0, y = 2.0, z = 3.0});
    return 0;
}