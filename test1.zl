struct Person {
    name arr<u8, 15>,
    age i32,
    is_male bool,
    id u64
}

fun main() >> i32 {
    arr<Person, 2> people;
    ptr<u8> name_ptr = &people[0].name[0];
    @scanf("%s", name_ptr);
    @printf("%s\n", people[0].name);
    return 0;
}