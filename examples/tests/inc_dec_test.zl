fun main() >> i32 {
    i32 a = 0;
    a++;
    @printf("(expected 1):%d\n", a);
    a--;
    a--;
    f16 fs = 1.0;
    f32 fb = 1.2;
    f64 fl = 1.3;
    @printf("(expected -1):%d\n", a);
    for i8 i = 0; i < 10; i++ {
        a = a + 10;
        fs++;
        fb++;
        fl++;
    }
    @printf("(expected 99):%d\n", a);
    f32 casted_fs = fs;
    @printf("(expected 11.0):%.1f\n", casted_fs);
    @printf("(expected 11.2):%.1f\n", fb);
    @printf("(expected 11.3):%.1f\n", fl);
    return 0;
}