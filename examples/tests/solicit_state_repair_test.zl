error NeedSeed = _;

fun next_value() >> i32 {
    i32 seed_rng = 0;
    solicit NeedSeed;
    return seed_rng + 41;
}

fun main() >> i32 {
    i32 out = next_value() on solicit NeedSeed {
        seed_rng = 1;
    };

    @printf("(expected 42):%d\n", out);
    return 0;
}
