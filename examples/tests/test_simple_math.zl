use std.math

fun main() >> i32 {
    @printf("=== Testing std.math module ===\n");
    
    ?? Test basic functions
    f64 x = 16.0;
    f64 result = sqrt(x);
    @printf("sqrt(%.1f) = %.2f (expected 4.00)\n", x, result);
    
    f64 power = pow(2.0, 3.0);
    @printf("pow(2.0, 3.0) = %.1f (expected 8.0)\n", power);
    
    ?? Test helper functions
    f64 sq = square(5.0);
    @printf("square(5.0) = %.1f (expected 25.0)\n", sq);
    
    f64 cb = cube(3.0);
    @printf("cube(3.0) = %.1f (expected 27.0)\n", cb);
    
    f64 neg_float = -5.5;
    f64 abs_float = fabs(neg_float);
    @printf("fabs(%.1f) = %.1f (expected 5.5)\n", neg_float, abs_float);
    
    ?? Test trigonometry
    f64 angle_rad = toRadians(90.0);
    f64 sine = sin(angle_rad);
    @printf("sin(90°) = %.6f (expected ~1.000000)\n", sine);
    
    f64 cosine = cos(0.0);
    @printf("cos(0°) = %.6f (expected 1.000000)\n", cosine);
    
    ?? Test distance function
    f64 dist = distance(0.0, 0.0, 3.0, 4.0);
    @printf("distance(0,0 to 3,4) = %.1f (expected 5.0)\n", dist);
    
    @printf("\n=== Math tests completed! ===\n");
    return 0;
}
