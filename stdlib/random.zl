wrap @srand(seed: u32) >> void;
wrap @rand() >> i32;

fun randRange(min: i32, max: i32) >> i32 {
    if (max < min) {
        return min;
    }
    i32 range = max - min + 1;
    return min + (rand() % range);
}
